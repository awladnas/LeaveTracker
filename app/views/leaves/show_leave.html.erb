<h1><%= params[:type] %> Leave Details</h1>

<div class="col-lg-12">
  <div class="info">
   <div> <span> Total Remaining: <%= get_left_leave_hours(params[:type], current_user)%> Hours</span> </div>
    <span> Total Consumed: <%= get_consumed_leave_hours(params[:type], current_user)%> Hours</span>
  </div>
  <table class="table table-bordered">
    <thead>
    <th>Start Date</th>
    <th>End Date</th>
    <th>Status</th>
    </thead>
    <% @leaves.each do |leave| %>
    <tr>
      <td><%= leave.start_date %></td>
      <td><%= leave.end_date %></td>
      <td><%= leave.leave_status %> </td>
    </tr>
    <% end %>
  </table>
</div>